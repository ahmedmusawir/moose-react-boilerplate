/*
 * MediaDcPage Messages
 *
 * This contains all the text for the MediaDcPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MediaDcPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MediaDcPage container!',
  },
});
